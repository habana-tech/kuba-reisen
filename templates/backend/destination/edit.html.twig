{% extends 'base.html.twig' %}

{% block title %}Edit Destination{% endblock %}

{% block body %}
    <h1>Editing Destination <i><u>{{ destination.name }}</u></i></h1>

    {{ include('backend/destination/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('backend_destination_index') }}" class="btn">back to list</a>

    {{ include('backend/destination/_delete_form.html.twig') }}
{% endblock %}
