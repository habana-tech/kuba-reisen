{% extends 'base.html.twig' %}

{% block title %}Destination{% endblock %}

{% block body %}
    <h1>Destination</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ destination.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ destination.name }}</td>
            </tr>
            <tr>
                <th>Language</th>
                <td>{{ destination.language }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_destination_index') }}">back to list</a>

    <a href="{{ path('backend_destination_edit', {'id': destination.id}) }}">edit</a>

    {{ include('backend/destination/_delete_form.html.twig') }}
{% endblock %}
