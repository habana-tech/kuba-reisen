{% extends 'base.html.twig' %}

{% block title %}Edit FilterTag{% endblock %}

{% block body %}
   <div class="row">
    <div class="grid-three-fourths">
        <h3>Editing tag <u><i>{{filter_tag.title}}</i></u> 🔖</h3>
    </div>
</div>
{{ form_start(form) }}
<div class="row" style="margin-bottom: 4em;">
    <div class="grid-half">
        {{ form_row(form.title) }}
    </div>
</div>
<div class="row" style="margin-bottom: 4em;">
    <div class="grid-half tags">
        {{ form_widget(form.pinned) }}
    	{{ form_label(form.pinned) }}
    </div>
</div>


<div class="row margin-top" style="">
	
	
            <button class="button button-primary button-shadow button-block">{{ button_label|default('Update') }}</button>
{{ form_end(form) }}

        <a href="{{ path('backend_filter_tag_index') }}" class="button button-primary button-shadow button-block">Go back to list</a>
    	{{ include('backend/filter_tag/_delete_form.html.twig') }}	
</div>


{% endblock %}
